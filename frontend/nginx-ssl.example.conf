# Exemple TLS pour production — montez vos certificats et activez ce bloc dans nginx.conf
#
# server {
#     listen 443 ssl http2;
#     server_name entomo.example.gov.sn;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#
#     # ... même configuration que nginx.conf (root, proxy /api/, /uploads/)
# }
